---
type: wayfind-map
status: working                 # convergence is COMPUTED by the frontier script, never stored
destination: "one-line pinned scope"   # editable at chart
created: 2026-07-16
context_pointers: []            # paths/globs the blind-spot pass sweeps (set at chart, editable)
references: []                  # provenance; bridges into the graduated feature's sources[]
---

## Destination

The documented spec-template frontmatter carrying its inline `#` comments, with
real (non-placeholder) values. It MUST validate (exit 0) — a quote-aware
trailing-comment strip removes each ` #…` before shape-matching.

## Not yet specified

- Nothing; this is a comment-stripping regression fixture.
