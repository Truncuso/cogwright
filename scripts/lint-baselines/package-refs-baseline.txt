# package-refs-baseline.txt — known-dangling package-side refs, sorted `<from>::<path>`.
#
# REMOVAL-ONLY ratchet, same contract as reference-manifest.baseline: ci-lints.sh
# (section `package-refs`) fails on an unbaselined dangling ref AND on an entry
# whose ref resolves again. Paths are relative to GF_PACKAGE_ROOT
# (default packages/goalforge). Every entry names the work that retires it.
