{
  "skill_name": "goalforge-execute",
  "evals": [
    {
      "id": 1,
      "prompt": "Execute wp-01-api-scaffold (status: ready) — full sub-cycle",
      "expected_output": "Sets status: executing on entry. For each task: ensures clean tree, picks next task honoring depends_on + parallel, stubs goalforge-pick-agent (WP-04), dispatches via implement, writes checkpoint, runs eval pass (verify + lint + type-check) with up to 3 retries before escalation, delegates simplification to verify-and-simplify, re-runs eval, commits atomically, sets task status: verified. Parallel tasks use EnterWorktree per task dispatched in one batched message; orchestrator merges with conflict resolution. Resume is idempotent.",
      "files": ["evals/fixtures/wp-ready/overview.md"],
      "expectations": [
        "SKILL.md declares name: goalforge-execute",
        "SKILL.md precondition is status: ready",
        "SKILL.md entry action is status: executing",
        "SKILL.md state transition ready → executing documented",
        "SKILL.md state transition executing → verified documented",
        "SKILL.md documents git status --porcelain clean-tree check",
        "SKILL.md documents depends_on satisfaction before task dispatch",
        "SKILL.md reuses implement for code-writing (does not reimplement it)",
        "SKILL.md reuses verify-and-simplify for simplification pass (does not reimplement it)",
        "SKILL.md documents evaluation pass: verify command + lint + type-check",
        "SKILL.md documents bounded configurable retry cap (SDD_MAX_RETRIES, default 3) before escalation",
        "SKILL.md documents AskUserQuestion escalation on retry cap (never silent-pass)",
        "SKILL.md documents blocker appended to findings.md on retry cap",
        "SKILL.md documents re-run eval after simplification",
        "SKILL.md documents atomic conventional commit per task",
        "SKILL.md documents status: verified set only after eval+simplify sign off",
        "SKILL.md documents wave dispatch in single batched message for parallel: true tasks",
        "SKILL.md documents EnterWorktree per parallel task",
        "SKILL.md documents ExitWorktree after merge",
        "SKILL.md documents conflict resolution (no discard) with findings.md record",
        "SKILL.md documents checkpoint.last_step, specialist, model, worktree, discovered_by, resumable fields",
        "SKILL.md documents checkpoint written belt-and-suspenders (works without WP-05 hook)",
        "SKILL.md documents idempotent resume: verified tasks skipped as no-op",
        "SKILL.md documents WP-04 pick-agent stub with injected choice fallback",
        "SKILL.md documents no hard specialist default and no silent route fallback",
        "Fixture wp-ready/overview.md has status: ready"
      ]
    },
    {
      "id": 2,
      "prompt": "Resume goalforge-execute for a WP already at status: executing",
      "expected_output": "Reads checkpoint from each task-*.md. Tasks with status: verified are skipped (no-op). Tasks with a live worktree in checkpoint.worktree re-enter their worktree via EnterWorktree. Tasks without a checkpoint are treated as pending. Resume is idempotent.",
      "files": ["evals/fixtures/wp-ready/overview.md"],
      "expectations": [
        "SKILL.md documents re-entry path for executing WP",
        "SKILL.md documents skip of verified/committed tasks as no-op",
        "SKILL.md documents re-entering live worktree on resume",
        "SKILL.md documents resumable: true/false in checkpoint block",
        "SKILL.md states resume is idempotent"
      ]
    },
    {
      "id": 3,
      "prompt": "Evaluation pass fails 3 times for task-02 — what happens?",
      "expected_output": "After 3 failed retries (each re-dispatching implement with failure context), goalforge-execute appends a blocker to findings.md including the task name, failure output, and retry count. It then escalates via AskUserQuestion. It does NOT silently mark the task verified.",
      "files": [],
      "expectations": [
        "SKILL.md documents bounded configurable retry cap (SDD_MAX_RETRIES, default 3)",
        "SKILL.md documents blocker appended to findings.md on cap",
        "SKILL.md documents AskUserQuestion escalation on cap",
        "SKILL.md explicitly states never silent-pass"
      ]
    },
    {
      "id": 4,
      "prompt": "Wave of 3 parallel tasks — how are worktrees managed?",
      "expected_output": "All 3 tasks dispatched in ONE batched message via superpowers:dispatching-parallel-agents. Each task gets its own EnterWorktree; path recorded in checkpoint.worktree before dispatch. After all subagents return, orchestrator merges each worktree. On merge conflict: resolve (never discard), record in findings.md. ExitWorktree per task after merge.",
      "files": [],
      "expectations": [
        "SKILL.md documents superpowers:dispatching-parallel-agents for wave dispatch",
        "SKILL.md documents superpowers:using-git-worktrees for isolation",
        "SKILL.md documents single batched message for full wave",
        "SKILL.md documents EnterWorktree per parallel task with path in checkpoint",
        "SKILL.md documents ExitWorktree after merge",
        "SKILL.md documents conflict resolution policy: resolve, do not discard",
        "SKILL.md documents recording conflict resolution in findings.md"
      ]
    },
    {
      "id": 5,
      "prompt": "WP goal uses strategy: deterministic — how does the outer loop decide completion?",
      "expected_output": "At entry, resolve_effective_goal yields the effective goal. After the sub-cycle, the pure goalforge-goal-eval runs the deterministic check; exit 0 → verdict.met=True → break and proceed to Step 10 (goalforge-verify). No dispatch needed.",
      "files": ["evals/fixtures/goal-loop/wp-met-deterministic.md"],
      "expectations": [
        "SKILL.md documents resolve_effective_goal at entry (Step 0b)",
        "SKILL.md documents pure goalforge-goal-eval invoked each outer iteration",
        "goalforge-goal-eval returns met=True in-script for a passing deterministic check (behavioral)"
      ]
    },
    {
      "id": 6,
      "prompt": "WP goal uses strategy: judge — how is completion decided without the script dispatching?",
      "expected_output": "goalforge-goal-eval returns met=null plus a {dispatch: judge, artifact, rubric, block_on} directive (the script stays pure). The agent invokes the judge skill and maps the verdict: met iff no finding at/above a block_on severity. In evals the judge dispatch is MOCKED for determinism.",
      "files": ["evals/fixtures/goal-loop/wp-judge.md"],
      "expectations": [
        "SKILL.md documents agent acting on the judge dispatch directive",
        "SKILL.md documents met-mapping via block_on severities",
        "goalforge-goal-eval returns met=null + dispatch directive; mocked block_on mapping yields met=True with no findings, met=False with a HIGH finding (behavioral)"
      ]
    },
    {
      "id": 7,
      "prompt": "A goal is never met across all outer iterations — what happens (blocked_stop)?",
      "expected_output": "Each outer iteration goalforge-goal-eval returns met=False. After outer_max_iter (or when the blocked_stop condition is hit), the agent appends a blocker to findings.md and escalates via AskUserQuestion. It never silently advances status.",
      "files": ["evals/fixtures/goal-loop/wp-blocked-stop.md"],
      "expectations": [
        "SKILL.md documents blocked_stop escalation path",
        "SKILL.md documents outer_max_iter bound independent of inner 3-retry",
        "goalforge-goal-eval returns met=False each iteration for the blocked-stop fixture (behavioral)"
      ]
    },
    {
      "id": 8,
      "prompt": "A WP sets inherits_from — how is its effective goal resolved (cascade)?",
      "expected_output": "resolve_effective_goal merges the WP goal with the inherited spec: outcome/verification are never inherited (WP's own); constraints/boundaries union-with-dedupe; scalars (iteration_policy, blocked_stop) inherited when unset on the WP. Single owner — goalforge-execute does not re-derive cascade.",
      "files": ["evals/fixtures/goal-loop/wp-cascade.md", "evals/fixtures/goal-loop/feature-spec.md"],
      "expectations": [
        "SKILL.md documents per-field cascade via resolve_effective_goal (single owner)",
        "resolved goal keeps WP outcome, unions constraints (WP ∪ spec), inherits unset scalars/boundaries (behavioral)"
      ]
    }
  ]
}
