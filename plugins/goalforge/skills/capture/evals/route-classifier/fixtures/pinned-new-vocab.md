---
name: pinned-new-vocab
route: wave
status: draft
---

<!-- Fixture (case d): PINNED NEW-vocab route (wave — NOT fast, so the
     idempotent echo path must generalize beyond the old default). Body carries
     only a small clean goal, yet the pin dominates. Expect:
     goalforge-route.sh -> route: wave, confidence: pinned (echo-as-is). -->

## Goal

anything — the pinned route wins over body signals.
