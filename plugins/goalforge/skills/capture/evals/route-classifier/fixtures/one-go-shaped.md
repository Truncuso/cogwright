---
name: one-go-shaped
route: fast
status: draft
---

<!-- Fixture (case e): ONE-GO-SHAPED — a smallest-unit feature expressed as
     route: fast whose execution_plan sets dispatch: agent on the implement step
     (exactly one implement-agent), NO spec step anywhere. one-go is never a 4th
     enum value — it is route: fast + a single-agent dispatch shape.
     Classifies to route: fast, confidence: pinned; --emit one-go-shaped derives
     the fast/one-go execution_plan (implement: agent). -->

## Goal

build the whole thing in one go with a single implement agent, no spec split.
