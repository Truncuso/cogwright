---
name: standard-canonical
status: draft
---

<!-- Fixture (case a): canonical STANDARD — a strong signal trips (>= NQ open
     questions => R3). Expect: goalforge-route.sh -> route: standard,
     confidence: clear. Also the --emit standard source (execution_plan derived
     from the classified route). -->

## Problem

Redesign the settings surface across several screens.

## Goal

settings model reworked with a migration path and updated UI.

## Open Questions

- how do we migrate existing user prefs?
- what is the rollback story?
- do we version the settings schema?
